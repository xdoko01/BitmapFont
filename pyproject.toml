[project]
name = "pgbitmapfont"
version = "0.1.6"
description = "A Python library to render text from bitmap font images"
authors = [
    { name = "xdoko01" }
]
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.7"
dependencies = [
    "pygame-ce",
    "pathlib"
]

[project.scripts]
bitmapfont-extract = "pgbitmapfont.extractor:main"

[project.urls]
Homepage = "https://github.com/xdoko01/Pygame-BitmapFont"
Issues = "https://github.com/xdoko01/Pygame-BitmapFont/issues"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
